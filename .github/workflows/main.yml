name: Run_CI
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: "Run test"
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Checkout
        run: |
          git https://github.com/Biaogo/auto_package.git
          .\auto_package\uup_download_windows.cmd