name: Run_CI
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    - name: clone git
      runs-on: self-hosted
  steps:
    - name: Checkout
      run: |
        git clone https://github.com/Biaogo/auto_package.git
        .\auto_package\uup_download_windows.cmd